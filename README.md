# tasktree

An async behavior tree runtime for Rust — nodes are async functions or structs, state is shared through a typed blackboard, trees can be composed via a fluent builder API or loaded from YAML.
